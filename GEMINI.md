# Lx Clínicas — contexto para o agente

Leia `CONTINUIDADE.md` antes de qualquer alteração: é o estado atual do projeto,
com as decisões já tomadas e as pegadinhas conhecidas (schema do banco, enums,
formato de telefone, validação de IDs).

As regras de trabalho estão em `AGENTS.md` e valem aqui integralmente —
em especial: **atualizar `CONTINUIDADE.md` antes de todo `git push`**
(o hook `.githooks/pre-push` bloqueia o push sem isso).
