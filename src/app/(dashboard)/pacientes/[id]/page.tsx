import { PageHeader } from "@/components/page-header";import { StatusBadge } from "@/components/status-badge";import { patients } from "@/lib/mock-data";import { CalendarPlus,MessageCircle,Mail,Phone } from "lucide-react";
export default async function Patient({params}:{params:Promise<{id:string}>}){const{id}=await params;const p=patients.find(x=>x.id===id)??patients[0];return <><PageHeader title={p.name} description={`Paciente desde 12/03/2024 • ${p.type}`} action={<div className="flex gap-2"><button className="button"><MessageCircle size={16}/>WhatsApp</button><button className="button button-primary"><CalendarPlus size={16}/>Novo agendamento</button></div>}/><div className="mb-4 flex gap-1 overflow-auto border-b border-[var(--border)]">{["Resumo","Agendamentos","Financeiro","Documentos","Histórico"].map((x,i)=><button className={`px-4 py-3 text-sm font-semibold ${i===0?"border-b-2 border-[var(--primary)] text-[var(--primary)]":"muted"}`} key={x}>{x}</button>)}</div><div className="grid gap-4 lg:grid-cols-[1.5fr_1fr]"><section className="panel p-5"><h2 className="font-bold">Dados pessoais</h2><div className="mt-5 grid gap-5 sm:grid-cols-2"><div><p className="text-xs muted">Nome completo</p><p className="mt-1 text-sm font-medium">{p.name}</p></div><div><p className="text-xs muted">CPF</p><p className="mt-1 text-sm font-medium">123.456.789-00</p></div><div><p className="text-xs muted">Telefone</p><p className="mt-1 flex items-center gap-2 text-sm"><Phone size={14}/>{p.phone}</p></div><div><p className="text-xs muted">E-mail</p><p className="mt-1 flex items-center gap-2 text-sm"><Mail size={14}/>paciente@email.com</p></div><div><p className="text-xs muted">Convênio</p><p className="mt-1 text-sm">{p.insurance}</p></div><div><p className="text-xs muted">Status</p><p className="mt-1"><StatusBadge status={p.status}/></p></div></div></section><section className="panel p-5"><h2 className="font-bold">Próxima consulta</h2><p className="mt-4 text-lg font-bold text-[var(--primary)]">21 de julho, 08:00</p><p className="mt-1 text-sm">Avaliação • Dra. Marina Lopes</p><p className="mt-4 text-xs muted">Clínica Sorriso — Unidade Centro</p></section><section className="panel p-5 lg:col-span-2"><h2 className="font-bold">Indicadores</h2><div className="mt-4 grid grid-cols-2 gap-4 sm:grid-cols-5">{[["Consultas","14"],["Última","08/07/2026"],["Faltas","1"],["Cancelamentos","2"],["Pendência","R$ 0,00"]].map(([l,v])=><div key={l}><p className="text-xs muted">{l}</p><p className="mt-1 font-bold">{v}</p></div>)}</div></section></div></>}
